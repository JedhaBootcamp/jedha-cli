version: "3"

services:
  gash:
    container_name: gash
    image: jedha/lab-bypass-login
    hostname: jedha-bootcamp
    networks:
      lab-network:
        ipv4_address: 10.10.3.78

networks:
  lab-network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 10.10.3.0/24
